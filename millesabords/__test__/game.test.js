describe('Given I am on the game pad', () => {
  test('I should see all the players with summury on the top of page', () => {
    //test
  });
  test('I should see the others players playing', () => {
    //test
  });
  describe('When it is my turn', () => {
    test('I should see the button "pick a card"', () => {
      //test
    });
    describe('When I get my card', () => {
      test('I should see the button "drop the dices', () => {
        //test
      });
      describe('When I drop the dice', () => {
        test('I should see the result of 8 dices', () => {
          //test
        });
      });
    });
  });
});
