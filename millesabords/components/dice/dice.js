import React from 'react';

const Dice = (props) => {
  const imgPath = '';
  const diceName = '';
  return (
    <li className="dice-el turn">
      i am a dice
      <img src={imgPath} alt={diceName} />
    </li>
  );
};

export default Dice;
