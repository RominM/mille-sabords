{
  "name": "@rf/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Front Reckless Fathoms — Nuxt 4 + SCSS (design system + écrans de jeu)",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "assets": "node scripts/optimize-images.mjs && node scripts/optimize-audio.mjs",
    "assets:images": "node scripts/optimize-images.mjs",
    "assets:audio": "node scripts/optimize-audio.mjs"
  },
  "dependencies": {
    "@rf/engine": "*",
    "@rf/protocol": "*"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "nuxt": "^4.0.0",
    "sass": "^1.80.0",
    "sharp": "^0.35.3",
    "typescript": "^5.5.0",
    "vue": "^3.5.0",
    "vue-tsc": "^2.0.0"
  }
}
